<%= form_for(estates) do |f| %>
  <% if estate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estate.errors.count, "error") %> prohibited this estate from being saved:</h2>

      <ul>
      <% estate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :rent %>
    <%= f.number_field :rent %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div class="field">
    <%= f.label :note %>
    <%= f.text_area :note %>
  </div>

  <hr>
  <% n = 0 %>
  <%= f.fields_for :stations do |f| %>
    <%= "最寄り駅#{n += 1}" %>

    <div class="field">
      <%= f.label :station_line %>
      <%= f.text_field :line %>
    </div>

    <div class="field">
      <%= f.label :station_name %>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :station_minute %>
      <%= f.number_field :minute %>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
